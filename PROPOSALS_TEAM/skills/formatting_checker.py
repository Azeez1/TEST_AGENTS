from typing import Dict

def check(doc_path: str, rules: Dict) -> Dict:
    # Placeholder: implement page count, font, margins using PDF/DOCX libs
    return {
        "page_limit_ok": True,
        "font_ok": True,
        "margins_ok": True,
        "filenames_ok": True,
        "notes": []
    }
